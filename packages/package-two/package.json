{
  "name": "package-two",
  "version": "1.0.0-beta.1",
  "description": "Multiply with self twice",
  "scripts": {
    "build": "echo \"Building package two!\" && exit 0",
    "test:unit": "jest"
  },
  "dependencies": {
    "package-one": "workspace:*"
  }
}
